import javax.swing.JOptionPane;

public class exercicio3 {
    public static void {String [] args} {
        int[][] mat = new int [4][4];
        int prodprinc = 1, prodsec = 1, diferença;
        for(int i = 0;i<4;i++)
        {
            for (int j = 0; j < 4,j++)
            // um for pra entrada
            {
                mat[i][j] = Integer.parseInt(JOptionPane.showInputDialog("entrada de numeros"))
            // um for pra saída
                for(int i = 0;i<4;i++)
                {
                    for (int j = 0; j < 4,j++)
                    // um for pra entrada
                    {
                        mat[i][j] = Integer.parseInt(JOptionPane.showInputDialog("saída"+mat)
                    }

            }
        }

    }
}
